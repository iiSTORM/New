{
  "name": "esports-kill-projector",
  "private": true,
  "description": "Build tooling for the front end. The app itself ships as a single self-contained index.html and has no runtime dependencies beyond React from a CDN.",
  "scripts": {
    "build": "node build/build-frontend.js",
    "check": "node build/build-frontend.js --check"
  },
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-react": "^7.27.1"
  }
}
